package util;

public class StringUtils {

    public static boolean isEmpty(String param) {
        if (param == null || param.isEmpty()) {
            return true;
        }

        param.isEmpty();
        return false;
    }

}
